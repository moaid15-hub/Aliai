# Replace with your real domain for the API endpoint
api.oqool.net {
	encode gzip
	@health {
		path /health
	}
	reverse_proxy api:8000
	# Optional: tighten timeouts, headers etc.
	# header {
	#   Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
	# }
}