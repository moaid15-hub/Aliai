version: 1version: 1

frontend:frontend:

  phases:  phases:

    preBuild:    preBuild:

      commands:      commands:

        - npm ci        - npm ci

    build:    build:

      commands:      commands:

        - npm run build        - npm run build

  artifacts:  artifacts:

    baseDirectory: out    baseDirectory: .next

    files:    files:

      - '**/*'      - '**/*'

  cache:  cache:

    paths:    paths:

      - node_modules/**/*      - node_modules/**/*

      - .next/cache/**/*